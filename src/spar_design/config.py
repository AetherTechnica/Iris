"""
config.py — Iris 設計定数・デフォルトパラメータ
================================================
全て mm / N·mm² / kg 単位で記述する。
空力計算（AerodynamicsAnalyzer）との境界のみm↔mm変換を行う。
"""

# -------------------------------------------------------
# スパン方向計算グリッド
# -------------------------------------------------------
GRID_SIZE: int = 50          # mm（台形則の刻み幅）

# -------------------------------------------------------
# かんざし継手
# -------------------------------------------------------
OVERLAP_LENGTH: int = 250    # mm（全継手で固定）
CLEARANCE: float    = 0.2    # mm（嵌合クリアランス）
BALSA_FILL_LENGTH: int = 100 # mm（径差補填バルサの長さ）

# -------------------------------------------------------
# 二次構造
# -------------------------------------------------------
RIB_PITCH: int  = 250        # mm
RIB_WEIGHT: float = 0.0178   # kg/個
RHO_BALSA: float  = 150e-9   # kg/mm³（バルサ密度）

# -------------------------------------------------------
# 材料制約
# -------------------------------------------------------
BUCKLING_LIMIT: float = 150.0  # D/t 最大値（座屈制約）
MAX_PLY_PER_LAYER: int = 20    # 各プライの最大積層数

# -------------------------------------------------------
# 収束ループ
# -------------------------------------------------------
MAX_ITER: int   = 50
CONV_TOL: float = 1e-3   # kg
RELAX_ALPHA: float = 0.5  # 緩和係数初期値

# -------------------------------------------------------
# デフォルト機体パラメータ（変更可能）
# -------------------------------------------------------
DEFAULT_SPAN: int        = 10000   # mm（片翼）
DEFAULT_ROOT_CHORD: int  = 300     # mm
DEFAULT_DELTA_MAX: int   = 2000    # mm（翼端たわみ許容）
DEFAULT_RHO_SKIN: float  = 0.05    # kg/m²
DEFAULT_BETA: float      = 0.9     # TR-797循環分布係数
DEFAULT_PAYLOAD: float   = 70.0    # kg（パイロット＋機構）
DEFAULT_W_INITIAL_SPAR: float  = 10.0  # kg（桁重量初期推定）
DEFAULT_W_INITIAL_SECONDARY: float = 5.0  # kg（二次構造初期推定）
